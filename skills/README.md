# Skills

This repository ships Agent Skills compatible with the Agent Skills specification.

Install skills from this repo:

```bash
npx skills add localixai/n8n-sdk
```

Included skills:

- `n8n-sdk-workflow-backend`
- `n8n-sdk-release-docs`

Each skill directory contains a `SKILL.md` file with metadata and operational instructions.
